{% extends 'base.html.twig' %}

{% block title %}Наши услуги{% endblock %}

{% block page_title %}Наши услуги{% endblock %}

{% block page_content %}
    <div class="container">
        {% if available | length %}
            <div class="row justify-content-start">
                {% for service in available %}
                    <div class="card col-4 mb-2" style="width: 18rem;">
                        <img class="card-img-top" src="{{ service.image }}" alt="Card image cap">
                        <div class="card-body">
                            <h5 class="card-title">{{ service.name }}</h5>
                            <p class="card-text">{{ service.description }}</p>
                            <a href="{{ path('order_creation', {'service' : service.id}) }}" class="btn btn-primary">Выбрать</a>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <p class="text-center">Извините, в данный момент нет доступных услуг</p>
        {% endif %}
        {% if unavailable | length %}
            <p class="text-center">
                Обычно у нас также доступны следующий услуги, но в данный момент мы не можем их выполнить.
                Уточняйте все подробности по контактному телефону
            </p>
            <div class="row justify-content-start">
                {% for service in unavailable %}
                    <div class="card col-4 mb-2" style="width: 18rem;">
                        <img class="card-img-top" src="{{ service.image }}" alt="Card image cap">
                        <div class="card-body">
                            <h5 class="card-title">{{ service.name }}</h5>
                            <p class="card-text">{{ service.description }}</p>
                            <a href="#" class="btn btn-primary" disabled>Выбрать</a>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>
{% endblock %}